"use client";
import { useState } from "react";
import LaptopArtwork from "@/app/components/LaptopArtwork";
export default function ProductGallery({images,name,brand,category}:{images:string[];name:string;brand?:string|null;category?:string|null}){const [active,setActive]=useState(0); const valid=images?.filter(Boolean)??[]; if(!valid.length)return <div className="flex aspect-square items-center justify-center rounded-3xl border border-line bg-surface p-10"><LaptopArtwork brand={brand} category={category} /></div>; return <div><div className="overflow-hidden rounded-3xl border border-line bg-surface"><img src={valid[active]} alt={name} className="aspect-square h-full w-full object-contain"/></div><div className="mt-3 grid grid-cols-5 gap-2">{valid.slice(0,5).map((src,i)=><button key={src} onClick={()=>setActive(i)} className={`overflow-hidden rounded-xl border ${active===i?'border-blue ring-2 ring-blue/10':'border-line'}`}><img src={src} alt="" className="aspect-square w-full object-cover"/></button>)}</div></div>}
