"use client";
import Link from "next/link"; import {useMemo,useState} from "react"; import type {Laptop} from "@/app/lib/types";
export default function SearchResults({laptops,initialQuery}:{laptops:Laptop[];initialQuery:string}){const[q,setQ]=useState(initialQuery);const results=useMemo(()=>{const x=q.toLowerCase().trim();return laptops.filter(l=>!x||[l.name,l.brand,l.model,l.cpu,l.ram,l.storage,l.gpu,l.category,l.sku].filter(Boolean).join(' ').toLowerCase().includes(x))},[laptops,q]);return <div className="mt-7"><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="شو عم تدور؟" className="w-full rounded-2xl border border-line bg-surface px-5 py-4 text-sm outline-none focus:border-blue"/><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{results.map(l=><Link key={l.id} href={`/laptops/${l.id}`} className="rounded-2xl border border-line bg-white p-4 hover:border-blue"><div className="flex gap-3"><div className="h-20 w-20 shrink-0 overflow-hidden rounded-xl bg-surface">{l.images?.[0]&&<img src={l.images[0]} alt="" className="h-full w-full object-cover"/>}</div><div className="min-w-0"><h3 className="font-bold text-ink">{l.name}</h3><p className="mt-1 text-xs text-ink-soft">{[l.cpu,l.ram,l.storage].filter(Boolean).join(' • ')}</p><p className="mt-2 font-extrabold">${l.price}</p></div></div></Link>)}</div>{!results.length&&<div className="mt-6 rounded-2xl bg-surface p-10 text-center text-sm text-ink-soft">ما في نتائج مطابقة.</div>}</div>}
